// const createUser = (name, age) => {
//   return { userName: name, userAge: age };
// };

const createUser = (name, age) => {
  return { userName: name, userAge: age };
};

// const createUser1 = () => {{}}
