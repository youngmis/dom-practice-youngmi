function greet(name) {
  // name = parameter (매개변수)
  console.log("안녕하세요, " + name + "님!");
}

greet("홍길동"); // '홍길동' = argument (인자, 인수)

// 홍길동이라는 인자가, name이라는 매개변수로 전달이 됐음

// Parameter (매개변수):
// - 함수 정의에 있는 변수
// - 형식 매개변수

// Argument (인자):
// - 함수 호출 시 전달하는 실제 값
// - 실제 매개변수
